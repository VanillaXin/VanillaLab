# 香草芯设定库 · Agent 指引

- 设定正文：`content/**/*.banira`（不用 Markdown）
- 编写与格式说明：`content/guide.banira`（站内「编写 → 编写说明」）
- 茶系 slug：红茶 `vanilla-black-tea`，黑茶 `vanilla-dark-tea`；禁止 `red-tea` 或把黑茶写成 `black-tea`
- 协作记忆：`PROJECT_MEMORY.md`；与 `content/` 冲突时以 `content/` 为准
